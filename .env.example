# Required
GEMINI_API_KEY=your_gemini_api_key_here

# Optional (secondary LLM)
ANTHROPIC_API_KEY=your_claude_api_key_here

# LLM defaults
DEFAULT_LLM_PROVIDER=gemini

# Gemini 3 models (latest frontier models - Feb 2026)
# Pro: Best multimodal understanding for design/UX/synthesis/report generation
# Flash: Frontier intelligence at speed for functionality/performance/a11y/code
GEMINI_PRO_MODEL=gemini-3-pro-preview
GEMINI_FLASH_MODEL=gemini-3-flash-preview

# Claude (secondary option)
CLAUDE_MODEL=claude-sonnet-4-5-20250929

# Storage
STORAGE_DIR=./storage
DATABASE_URL=sqlite:///./data/gonogo.db

# Server
BACKEND_PORT=8000
FRONTEND_PORT=5173
CORS_ORIGINS=http://localhost:5173

# Scan limits
MAX_DEEP_PAGES=30
MAX_SHALLOW_PAGES=100
MAX_SCAN_DURATION_SECONDS=600
MAX_UPLOAD_SIZE_MB=10

# ============================================================================
# FIX LOOP — CYCLE CONTROL
# ============================================================================

# Maximum number of scan→fix→rescan cycles before stopping
# Range: 1-10, typically 2-3 cycles are sufficient
# DEFAULT_MAX_CYCLES=3

# Auto-stop when this verdict is reached
# Options: "GO", "GO_WITH_CONDITIONS", "never"
# - "GO": Stop when verdict reaches GO (recommended)
# - "GO_WITH_CONDITIONS": Stop when verdict reaches GO_WITH_CONDITIONS or better
# - "never": Run all cycles regardless of verdict (manual stop only)
# DEFAULT_STOP_ON_VERDICT=GO


# ============================================================================
# FIX LOOP — DEPLOYMENT & FIX APPLICATION
# ============================================================================

# How to deploy fixes for testing
# Options: "branch" (recommended), "main", "pr"
# - "branch": Create new branch per cycle, deploy that (safest)
# - "main": Deploy to main branch (not recommended)
# - "pr": Create PR per cycle (slower, more visibility)
# DEFAULT_DEPLOY_MODE=branch

# How to apply fixes to codebase
# Options: "branch" (recommended), "main", "commit"
# - "branch": Apply fixes to new branch (safest)
# - "main": Apply directly to main branch (dangerous!)
# - "commit": Create commits on current branch
# DEFAULT_APPLY_MODE=branch

# Prefix for auto-generated fix branches
# Example: "gonogo/fix-" → "gonogo/fix-cycle-1", "gonogo/fix-cycle-2"
# FIX_BRANCH_PREFIX=gonogo/fix-


# ============================================================================
# FIX LOOP — CLAUDE CODE INTEGRATION
# ============================================================================

# Path to Claude Code CLI binary
# Default "claude" assumes it's in your PATH
# Windows example: C:\Program Files\Claude\claude.exe
# CLAUDE_CODE_PATH=claude

# Maximum number of tool-use turns per fix session
# Claude Code operates in cycles: read → analyze → edit → verify
# 50 turns handles most moderate-complexity fixes
# CLAUDE_CODE_MAX_TURNS=50

# Timeout per fix session (seconds)
# Default: 600 seconds (10 minutes)
# CLAUDE_CODE_TIMEOUT_SECONDS=600

# Claude Code permission mode
# Options: "bypassPermissions" (recommended), "acceptEdits"
# CLAUDE_CODE_PERMISSION_MODE=bypassPermissions

# Allowed tools for Claude Code (only used when permission_mode != bypassPermissions)
# CLAUDE_CODE_ALLOWED_TOOLS=Read,Write,Edit,Bash(npm run *),Bash(npx *),Bash(git diff *),Bash(git status)

# Maximum budget per cycle (USD)
# Default: $5.00 per cycle (typically uses $0.10-$2.00 for normal fixes)
# CLAUDE_CODE_MAX_BUDGET_USD=5.0
